pipeline {
    agent {
      node {
          label "built-in"
          customWorkspace '/mnt/Roman'
      }
    }
    
    stages {
        stage ('delete folder') {
            steps {
                sh "rm -rf project"
            }
        }
    }
}
